# 2026-04-05 Daily Memory

## Session Summary

### Google Vertex Model Fix (In Progress)
- OAuth credentials ARE working — access tokens obtained from service account
- Vertex AI API (`aiplatform.googleapis.com`) is enabled on GCP project `aats-invoice-agent`
- BUT publisher model endpoints return 404 — `projects/aats-invoice-agent/locations/us-central1/publishers/google/models/*` not accessible
- Likely cause: Vertex AI project exists but Google's publisher model access is restricted (needs GCP console verification)
- Alternative path: Use `google` provider with Gemini API key from aistudio.google.com (no GCP console needed)

### Reminder Fired
- google-vertex/gemini-3.1-flash-lite check triggered: model NOT available in `models list --all` (exit code 1)
- No action taken — model unavailable, switching would break assistant

### GCP Project Context
- Project ID: `aats-invoice-agent`
- Project Number: `678422495474` (from OAuth error message)
- Env vars confirmed: `GOOGLE_CLOUD_PROJECT=aats-invoice-agent`, `GOOGLE_CLOUD_LOCATION=us-central1`, `GOOGLE_APPLICATION_CREDENTIALS=/home/logmover/.config/gcp/aats-invoice-key.json`

### OpenClaw Status
- Default model: `minimax/MiniMax-M2.7`
- google-vertex fallback: `google-vertex/gemini-3.1-pro`, `google-vertex/gemini-3-flash`, `google-vertex/gemini-2.5-flash-lite`
- google provider auth: NOT configured (no `google:default` profile)

## Session Summary (Afternoon Update — 2:42 PM)

### Local LLM Research (2 sessions)
- Logmover asked about latest local LLM news: covered Llama 3.3, Mistral Small 3, Phi-4-mini, Qwen 3, DeepSeek — all strong options for self-hosting
- Follow-up question on efficiency: covered quantization (AWQ 741 tok/s, 4-bit cuts memory 70%), KV cache compression (Google's TurboQuant, ICLR 2026), vLLM v0.16.0 with AMD ROCm/Intel XPU support, speculative decoding for 2x speed gains
- No action needed — research was informational

### OpenClaw Status
- Active model: MiniMax-M2.7 (confirmed with user)
- No model changes made

## TODO
- [ ] Add `google:default` auth profile with Gemini API key (user needs to provide)
- [ ] Enable Generative Language API on GCP project if needed
- [ ] Set up exec allowlist for heartbeat automation
- [ ] Consider local LLM setup (DeepSeek/Qwen) if Logmover wants a self-hosted option

## EOD Summary
Local LLM research conducted — Logmover explored local model options and efficiency tech (quantization, TurboQuant, vLLM updates). No system changes.
# 2026-04-05 Daily Memory

## Session Summary

### Google Vertex Model Fix (In Progress)
- OAuth credentials ARE working — access tokens obtained from service account
- Vertex AI API (`aiplatform.googleapis.com`) is enabled on GCP project `aats-invoice-agent`
- BUT publisher model endpoints return 404 — `projects/aats-invoice-agent/locations/us-central1/publishers/google/models/*` not accessible
- Likely cause: Vertex AI project exists but Google's publisher model access is restricted (needs GCP console verification)
- Alternative path: Use `google` provider with Gemini API key from aistudio.google.com (no GCP console needed)

### Reminder Fired
- google-vertex/gemini-3.1-flash-lite check triggered: model NOT available in `models list --all` (exit code 1)
- No action taken — model unavailable, switching would break assistant

### GCP Project Context
- Project ID: `aats-invoice-agent`
- Project Number: `678422495474` (from OAuth error message)
- Env vars confirmed: `GOOGLE_CLOUD_PROJECT=aats-invoice-agent`, `GOOGLE_CLOUD_LOCATION=us-central1`, `GOOGLE_APPLICATION_CREDENTIALS=/home/logmover/.config/gcp/aats-invoice-key.json`

### OpenClaw Status
- Default model: `minimax/MiniMax-M2.7`
- google-vertex fallback: `google-vertex/gemini-3.1-pro`, `google-vertex/gemini-3-flash`, `google-vertex/gemini-2.5-flash-lite`
- google provider auth: NOT configured (no `google:default` profile)

## Session Summary (Afternoon Update — 2:42 PM)

### Local LLM Research (2 sessions)
- Logmover asked about latest local LLM news: covered Llama 3.3, Mistral Small 3, Phi-4-mini, Qwen 3, DeepSeek — all strong options for self-hosting
- Follow-up question on efficiency: covered quantization (AWQ 741 tok/s, 4-bit cuts memory 70%), KV cache compression (Google's TurboQuant, ICLR 2026), vLLM v0.16.0 with AMD ROCm/Intel XPU support, speculative decoding for 2x speed gains
- No action needed — research was informational

### OpenClaw Status
- Active model: MiniMax-M2.7 (confirmed with user)
- No model changes made

## Session Summary (Afternoon Update — 3:49 PM)

### DeepSeek API Setup
- Logmover provided DeepSeek API key: `sk-086b33290375432a81f65fd77b01ea4a`
- Configured DeepSeek provider in OpenClaw config:
  - Base URL: `https://api.deepseek.com/v1`
  - Models: `deepseek-chat` (DeepSeek V3) and `deepseek-reasoner` (DeepSeek R1)
  - Pricing: $0.27/M input, $1.10/M output (V3); $0.27/M input, $2.19/M output (R1)
- Environment variable `DEEPSEEK_API_KEY` set in systemd user environment
- Gateway restarted successfully
- DeepSeek V3 set as default model (replacing MiniMax-M2.7)

### Model Comparison Discussion
- Provided detailed comparison of major LLMs:
  - Tier 1: Claude Opus 4.6 (best coding, reasoning), GPT-5.4 (best multimodal, agents), Gemini 3.1 Pro (best value + 2M context)
  - Tier 2: MiniMax M2.7 (current, good value), DeepSeek V3/R1 (best value, open-weights), Qwen 2.5 Coder (best local coding)
  - Tier 3: Llama 3.3, Phi-4-mini, Grok 3, Kimi K2.5 (niche/specialized)
- DeepSeek V3 noted as excellent value with strong coding capabilities

### Ollama Explanation
- Explained Ollama as local model runner (not for cloud models)
- Recommended for local deployment of models like DeepSeek Coder V2.5, Qwen 2.5 Coder
- Hardware requirements discussed (DeepSeek Coder V2.5 needs ~32GB RAM)

### Coding Tools Discussion
- Current tools: MiniMax-M2.7 for general code, OpenAI Codex (broken OAuth)
- ACP coding sessions available for agentic coding
- DeepSeek V3 now configured as primary coding model

### Context Window Check
- Current context usage: 39k/200k tokens (20%) — no issues

## TODO
- [ ] Add `google:default` auth profile with Gemini API key (user needs to provide)
- [ ] Enable Generative Language API on GCP project if needed
- [ ] Set up exec allowlist for heartbeat automation
- [ ] Test DeepSeek V3 performance for coding tasks
- [ ] Consider fixing OpenAI Codex OAuth for additional coding model option

## EOD Summary
DeepSeek V3 successfully configured as default model. Logmover now has a cost-effective, capable coding model alternative to MiniMax. System context window healthy at 20% usage. Gateway restarted with new provider config.
## Memory Configuration Fine-Tuning (5:16 PM)

### Article Review: OpenClaw Memory Masterclass
- Reviewed comprehensive memory optimization guide from https://velvetshark.com/openclaw-memory-masterclass
- Key recommendations implemented:

### Configuration Updates Applied
1. **Enhanced QMD Backend Configuration**:
   - Added `reserveTokensFloor: 40000` for DeepSeek V3 (200K context)
   - Enabled `preCompactionMemoryFlush: true` to save context before compaction
   - Added `sessionPruning: { mode: "cache-ttl", ttlSeconds: 86400 }` for 24-hour session pruning

2. **Updated AGENTS.md Memory Protocol**:
   - Added comprehensive memory protocol rules:
     - Before answering questions about past work → search memory first
     - Before starting any new task → check memory/today's date for active context
     - When learning something important → write to appropriate file immediately
     - When corrected on a mistake → add correction as rule to MEMORY.md
     - When session ending or context large → summarize to memory/YYYY-MM-DD.md

3. **Added Retrieval Protocol**:
   - Before non-trivial work: 1) memory_search for project/topic/user preference, 2) memory_get referenced file chunk if needed, 3) proceed with task

### Gateway Restart
- Restarted OpenClaw gateway to apply configuration changes
- Configuration now optimized for memory efficiency and context management

### Key Insights from Article
- QMD backend (Track B) is our current setup with `includeDefaultMemory: true`
- Memory scope rules already correct (direct chats only, not group chats)
- Compaction configuration was missing but now added
- Memory flush system now enabled to preserve important context
- Context pruning helps manage tool bloat and improves cache hit rates

### TODO
- [ ] Monitor memory performance with new configuration
- [ ] Test memory search functionality with QMD
- [ ] Verify compaction behavior during long sessions
- [ ] Check if GCP Generative Language API is still needed for QMD embeddings (project 315394193472)