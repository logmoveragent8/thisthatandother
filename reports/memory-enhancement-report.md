# OpenClaw Memory Enhancement Report

**Generated:** March 20, 2026  
**System:** Geekom XT13 Pro

---

## Executive Summary

Your OpenClaw instance has **QMD (Query-Memory-Default)** enabled, which is a good foundation. However, several advanced memory features are either missing or not fully configured. This report compares your current setup with available enhancements.

---

## Current Configuration

```json
"memory": {
  "backend": "qmd",
  "citations": "auto",
  "qmd": {
    "includeDefaultMemory": true,
    "update": { "interval": "5m", "debounceMs": 15000 },
    "limits": { "maxResults": 6, "timeoutMs": 4000 },
    "scope": {
      "default": "deny",
      "rules": [{ "action": "allow", "match": { "chatType": "direct" } }]
    }
  }
}
```

### What's Working ✅
- QMD backend enabled
- Daily memory files (`memory/YYYY-MM-DD.md`) being written
- Curated memory (`MEMORY.md`) in use
- Direct message scope properly restricted

### What's Missing ❌
- No embedding provider configured
- No hybrid search (BM25 + vector)
- No MMR diversity re-ranking
- No temporal decay
- Scope only allows direct messages (no group support)

---

## Available Enhancements

### 1. Embedding Provider Configuration

**Current:** Not configured (uses default)  
**Recommended:** Configure a local embedding provider to reduce API costs

| Provider | Cost | Quality | Setup Effort |
|----------|------|---------|--------------|
| **Ollama** (local) | Free | Good | Medium |
| **Voyage** (cloud) | Low | Excellent | Low |
| **Gemini** (cloud) | Included | Good | Low |
| **OpenAI** (cloud) | Medium | Excellent | Low |

**Recommendation:** Use Ollama for local embeddings (free, runs on Geekom)

```json
"embeddings": {
  "defaultProvider": "ollama",
  "ollama": {
    "baseUrl": "http://localhost:11434",
    "model": "nomic-embed-text"
  }
}
```

---

### 2. Hybrid Search (BM25 + Vector)

**Current:** Not enabled  
**Benefit:** Combines keyword search with semantic similarity for better recall

```json
"memory": {
  "backend": "qmd",
  "qmd": {
    "hybrid": {
      "enabled": true,
      "bm25Weight": 0.3,
      "vectorWeight": 0.7
    }
  }
}
```

---

### 3. MMR Diversity Re-ranking

**Current:** Not enabled  
**Benefit:** Prevents returning similar results, increases diversity

```json
"qmd": {
  "rerank": {
    "enabled": true,
    "provider": "ollama",
    "model": "bge-reranker-v2-m3"
  }
}
```

---

### 4. Temporal Decay

**Current:** Not configured  
**Benefit:** Prioritizes recent memories over old ones

```json
"qmd": {
  "temporalDecay": {
    "enabled": true,
    "halfLifeDays": 30
  }
}
```

---

### 5. Expanded Scope

**Current:** Only direct messages  
**Enhancement:** Enable for group chats and specific channels

```json
"scope": {
  "default": "deny",
  "rules": [
    { "action": "allow", "match": { "chatType": "direct" } },
    { "action": "allow", "match": { "channel": "telegram", "groupId": "allowed-group-id" } }
  ]
}
```

---

## Advanced Alternatives

### Engram (Drop-in Replacement)
- Full memory plugin with entity tracking
- Governance and multi-agent isolation
- Hybrid search via QMD
- **Repo:** github.com/joshuaswarren/openclaw-engram

### Cognee (Knowledge Graphs)
- Builds relationship graphs between memories
- Understands connections, not just similarity
- Good for complex retrieval needs

---

## Recommendations

| Priority | Enhancement | Effort | Impact |
|----------|-------------|--------|--------|
| **High** | Configure embedding provider | Low | High |
| **High** | Enable hybrid search | Low | Medium |
| **Medium** | Add temporal decay | Low | Medium |
| **Medium** | Enable MMR re-ranking | Medium | Medium |
| **Low** | Expand scope to groups | Low | Low |

---

## Next Steps

1. **Install Ollama** on Geekom (if not already):
   ```bash
   curl -fsSL https://ollama.com/install | sh
   ollama pull nomic-embed-text
   ```

2. **Update config** with embedding provider

3. **Enable hybrid search** in QMD settings

4. **Test** with `memory_search` queries

---

## Issues to Fix

⚠️ **Memory Search Currently Broken:** The memory_search function is returning errors due to missing Gemini embedding API access. Error message:
> "Generative Language API has not been used in project 315394193472 before or it is disabled"

**Fix options:**
1. Enable Generative Language API in Google Cloud Console, OR
2. Switch to local embeddings (Ollama), OR
3. Use Voyage/Mistral as embedding provider

---

*Report generated by Wukong 🐒*
